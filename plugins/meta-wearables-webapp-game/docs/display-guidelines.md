# Display guidelines

This document belongs to the base `meta-wearables-webapp` plugin, which this plugin builds on
and which publishes it directly — one copy, so the two cannot drift apart.

Read it at [`references/display-guidelines.md`](https://github.com/facebook/meta-wearables-webapp/blob/HEAD/plugins/meta-wearables-webapp/references/display-guidelines.md).
